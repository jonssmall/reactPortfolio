var React = require('react');
var PropTypes = React.PropTypes;

function Home(props) {
    return (
        <div>
            About, links & contact goe here.
        </div>
    )
};

module.exports = Home;